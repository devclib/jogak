packages:
  - 'packages/*'
  - 'apps/*'
  - 'benchmarks/baselines/*'
  - 'test-case/*'
# examples/*는 워크스페이스에서 제외 — 외부 개발자가 폴더만 복사해 npm install로 동작하도록.
# test-case/*는 jogak이 지원하는 framework별 실 사용 시나리오(쇼핑몰 도메인) 검증 영역.
# workspace에 포함 — @jogak/* workspace:* 캐럿으로 받아 즉시 시험 가능.
